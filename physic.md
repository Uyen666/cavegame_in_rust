# CaveGame 物理引擎技術總結 (Physics Engine Architecture)

CaveGame 實作了專為體素遊戲量身打造的特製物理引擎，結合了「連續碰撞偵測（CCD）」與「離散軸向分離」的混合設計，以達到媲美原版 Minecraft 的極致流暢度與防穿模穩定性。

## 1. 幾何核心與碰撞偵測 (AABB & Swept Collision)
* **Swept AABB 掃掠碰撞 (CCD)**：
  - 放棄了傳統的離散相交測試，採用高等級的 `Swept AABB`（掃掠軸對齊邊界框）演算法。
  - 系統會在玩家移動前，預先投射其速度向量（Velocity Vector），精準計算出與地形方塊發生碰撞的「確切時間點（Time of Impact, $t \in [0, 1]$）」與「碰撞法線（Normal）」。
  - **優勢**：即使玩家在極高速度下（例如自由落體或疾跑），也絕對不會發生穿透方塊（Tunneling）的致命 Bug。
* **Minecraft 標準安全緩衝 (MC_EPSILON)**：
  - 引入了 `1.0E-7` 的微小浮點數容差，取代傳統的 `== 0.0` 判斷，完美迴避了 IEEE 754 浮點數精度在邊界判定時造成的死鎖。

## 2. 運動學與碰撞消解 (Kinematics & Resolution)
* **離散軸向分離結算 (Axis-Separated Resolution)**：
  - 玩家的移動向量不會一次性在 3D 空間中解算，而是被嚴格拆解為 **X 軸 → Z 軸 → Y 軸** 三個獨立階段進行碰撞測試與位移應用。
  - **優勢**：當玩家斜向撞擊牆壁時，沿著牆壁的平行速度不會被抹除，達成了極度絲滑的「貼牆滑行（Wall Sliding）」手感，且徹底根除了在方塊轉角處卡頓（Corner Snagging）的通病。
* **微觀滲透防禦 (Anti-Penetration Margin)**：
  - 當玩家撞擊方塊並被系統彈回時，物理引擎會強制維持 `0.001` 格的安全距離外推。
  - **優勢**：根除「False Grounding（假性接地）」與「微觀穿模」，確保攝影機永遠不會意外看透實心方塊內部。

## 3. 玩家實體參數與互動 (Player Entity & Interaction)
* **動態 AABB 尺寸變換**：
  - **站立狀態**：半徑 `0.3` 格，高度 `1.8` 格。
  - **蹲下狀態 (Left Ctrl)**：半徑 `0.3` 格，高度動態縮小為 `1.2` 格，允許玩家鑽入 1.5 格高的狹窄坑洞，放開 Ctrl 即時恢復。
* **防卡死與安全網 (Safety Nets)**：
  - **第一幀地形安全鎖**：在玩家腳下的區塊完成非同步載入之前，系統會強制凍結玩家的重力牽引與位移，絕對防止玩家在進遊戲瞬間墜入虛空。
  - **反卡死救援 (Anti-Stuck Nudge)**：玩家的滑鼠交互系統中設有防護閘門，無法在自己身體的 AABB 內部放置方塊；若因地形生成或意外卡入實心方塊，系統會在單幀內自動施加溫和的向上推力（Upward Nudge），將玩家擠出實體地表。

## 4. 上帝視角與除錯介入 (Spectator & Debugging)
* **F4 旁觀者穿牆模式 (Spectator Mode)**：
  - 透過物理引擎最頂端的「短路接管（Short-Circuit Override）」實作。
  - 啟動後無視一切 AABB 碰撞、地形安全鎖與重力牽引，允許玩家透過 WASD 與 Space/Shift 進行絕對 3D 自由飛行（Ctrl 可疾跑提速）。
  - **雙向隔離**：在此模式下，滑鼠破壞/放置方塊的射線檢測權限會被系統徹底閹割，確保上帝視角僅供純粹的巡檢與截圖，絕不會意外破壞世界物理狀態。
