# 📒 Cavegame 更新日記 — 2026/08/08

---

## 🔧 今日重構與修復

### 1. 火把特化物件重構 (Torch Specialized Object Refactor)
徹底告別將火把當成 1x1x1 標準方塊處理的架構錯誤，從體素定義、物理包圍盒、光照渲染到面剔除進行全方位重構：

* **自訂 AABB 包圍盒 (`voxel.rs`)**：為 `BlockType` 新增 `get_aabb_offsets()` 方法。標準方塊回傳 `(0,0,0)-(1,1,1)` 滿版包圍盒；火把（含地面與牆面）回傳 `(0.375, 0.0, 0.375)-(0.625, 0.625, 0.625)` 的瘦長精準包圍盒。此方法作為後續鋼絲邊框、碰撞檢測等系統的統一介面。

* **動態鋼絲選中邊框 (`player/mod.rs`)**：修改目標方塊高亮線框 (Targeted Block Wireframe) 系統。原先對所有方塊繪製固定 1.002 倍的立方體邊框，現改為讀取 `target_block.get_aabb_offsets()` 動態計算 `center` 與 `size`，讓鋼絲邊框完美貼合火把的小木棍尺寸，不再是突兀的巨大正方體。

* **火把頭部自發光 Fullbright (`greedy.rs`)**：在 `push_torch_quads` 中，火把所有頂點的 `block_lights` 強制鎖定為 `[15, 15, 15, 15]`（滿亮）。火把本體永不接受環境光衰減，頭部火苗區域始終維持最高亮度，完美模擬自發光 (Emissive) 效果。

* **雙面獨立幾何 Double-Sided Geometry (`greedy.rs`)**：徹底廢除火把的鄰居面剔除 (Face Culling)。每個面生成正反兩組 Quad（正面 CCW + 背面翻轉法線），共 12 個 Quad。無論玩家從前、後、左、右、上、下任何角度觀察火把，所有面永遠可見，徹底消滅缺面穿幫問題。

### 2. 牆面火把 (Wall Torch) 定位與邊框修正
* **朝向感知包圍盒 (Direction-Aware AABB)**：擴充 `get_aabb_offsets()` 邏輯。牆面火把 (`TorchWallN/S/E/W`) 現會依據附著牆面回傳特化的斜向偏移包圍盒（例如 East 牆面回傳 `[0.5, 0.1, 0.35]-[1.0, 0.8, 0.65]`），使 Gizmo 鋼絲框能精準包覆傾斜狀態下的火把，解決高亮框錯位問題。
* **著色器網格幾何校正 (`voxel.wgsl`)**：修正了牆面火把的頂點變形邏輯。將牆面火把的 Y 軸基準線抬高 (`dy += 0.2`) 以緊密貼合牆壁而不懸浮；並優化了頂部火苗的向外傾斜比例 (`dy > 0.5` 時 `dx/dz -= flow * 0.25`)，展現完美的 15 度自然傾角。同時，將底座整體往牆內推移 0.08 單位（例如 `dx += flow * 0.45`），實現與牆體的完美嵌合，避免邊緣漏空。

### 3. 微型物件射線檢測與環境光遮蔽優化 (Sub-Block Raycast & AO Refinement)
* **二次交點檢測 (Ray-AABB Intersection)**：全面重構玩家的瞄準 (Highlight) 與互動 (Interaction) Raycast 步進演算法。現在射線不只看方塊佔用，還會**進階比對射線座標是否位於方塊的自訂 AABB 內**。如果射線穿過了火把所在的 1x1x1 網格但「未擊中火把的小型邊框」，射線將穿透火把並順利擊中後方的牆面！
* **禁用非不透明方塊 AO 陰影 (Disable Opaque AO Cast)**：在貪婪網格的光照採樣 `get_light_global` 中，將光照強制歸零（AO 陰影產生）的條件改為嚴格判斷 `block.is_opaque()`。現在如火把 (`Torch`) 與玻璃 (`Glass`) 等非實心方塊在平滑光照 (Smooth Lighting) 聚合時，會直接提供自己的環境光/方塊光數據，不再對周圍地形（例如其附著的木頭牆面）投射突兀的暗角陰影。

### 15:30 物理系統組件化 (Generic Physics & Entity System)
- 將 swept.rs 及運動學、碰撞消解邏輯從 player/mod.rs 抽離，建立獨立 PhysicsPlugin。
- 實作通用物理組件：RigidBody、AabbCollider、Velocity、GroundSensor、FluidSensor。
- 在 main.rs 中使用 .chain() 剛性鎖定 FixedUpdate 系統順序：輸入 -> 運動學 -> 碰撞消解。
- 實作三軸即時位置同步消解，防止斜向衝撞牆角穿模。
- 透過 RigidBody.is_kinematic 標記優雅支援旁觀者模式繞過物理判定。

### 15:45 細節優化與警告修復
- 移除了 RigidBody 中無用的 drag 屬性（因為水體阻力目前直接透過 config.physics.water_damping 計算）。
- 為未來擴充保留的 AabbCollider::new 加上了 #[allow(dead_code)] 標記。
- 確保 cargo check 編譯達到 0 warning、0 error 的完美大滿貫狀態。

### 15:55 重大 Bug 熱修復 (水體尖刺爆炸、F鍵錯位、浮力失效)
1. **水體網格尖刺與爆炸修復**：
   - 修正了 greedy.rs 中的 packed_data 打包位移量（Bit Shifts）以及基準常數（Base 33），使其完美對齊  oxel.wgsl 著色器的拆解邏輯。
   - 修正流體高度計算時當  alid_water_count == 0 時的回傳值由 8 改為 7，杜絕 3-bit (& 0x07) 溢位導致偏移歸零造成的滿版尖刺錯誤。
2. **F 鍵水體生成邏輯修正**：
   - 移除了過期且錯誤的 mark_chunk_dirty 呼叫。
   - 直接在 WorldManager::set_fluid_global 內部實作了完整的**邊界 Chunk 自動髒污標記（Dirty Marking）**邏輯。現在不論是放置方塊或水體，只要碰到 Chunk 邊緣（0 或 31），都會精確地觸發相鄰 Chunk 的重新網格化（Remeshing）。
3. **流體感測器與浮力修復**：
   - 擴展了 update_fluid_sensors 邏輯。現在不再只單點檢查玩家中心點，而是透過雙層迴圈掃描玩家整體的 AabbCollider 包圍盒覆蓋區，只要接觸到任何水體 (fluid_level > 0) 就會精確觸發浮力機制。確保玩家在水岸邊緣也能正確游泳，不再失靈。

### 16:05 數據驅動 Block & Item 註冊表 (Data-Driven Block & Item Registry)
* **結構化註冊表架構 (`src/world/registry.rs` & `src/item/mod.rs`)**：
  * 徹底淘汰 `BlockType` 中硬編碼的 `match` 分支，引進數據驅動的 `BlockDefinition` 與 `BlockRegistry` 系統。
  * 為方塊補齊了 `hardness`（硬度）、`required_tier`（適用工具層級）、`preferred_tool`（最適工具）、`drop_table`（破壞掉落物）與 `sound_category`（音效類別）等全方位屬性。
  * 新增 `SixFaces([u32; 6])` 貼圖映射變體，為未來帶朝向方塊（如熔爐、南瓜）做好幾何特化準備。
  * 引進 `ItemType`, `ItemKind` (Block/Tool/Material), `ItemDefinition` 與 `ItemRegistry` 物品系統，為工具耐久度、採掘效率與背包系統奠定堅實基礎。
* **全域 Static 表無鎖直尋 (Zero-Lock Static Lookup)**：
  * 將方塊定義託管於 `pub static BLOCK_DEFINITIONS: [BlockDefinition; 16]`。
  * `impl BlockType` 的固有方法（`is_solid`, `is_opaque`, `emitted_light`, `texture_mapping`, `get_aabb_offsets` 等）直接通過陣列索引 `self as usize` 實現 0 鎖定開銷、$O(1)$ 常數級直尋，完全無縫支援背景 Chunk 網格生成與多執行緒光照傳播。
* **貪婪網格生成器解耦 (`src/render/greedy.rs`)**：
  * 重構 `get_texture_layer` 函數，全面改呼叫 `block.texture_mapping().get_layer(d, normal)`，解決面貼圖硬編碼分支膨脹問題。
* **單元測試與 0 警告編譯驗證**：
  * 補齊 `BlockRegistry` 與 `ItemRegistry` 單元測試，確保 `cargo test` 全部通過，`cargo check` 達 0 錯誤、0 警告大滿貫。
